services:
  api:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - mealdeck-data:/app/data
    environment:
      MEALDECK_UPLOAD_DIR: /app/data/uploads
      OPENAI_API_KEY: ${OPENAI_API_KEY}

volumes:
  mealdeck-data:
